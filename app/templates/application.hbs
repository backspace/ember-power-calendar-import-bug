{{#power-calendar
  class='calendar'
  center=center
  onCenterChange=(action (mut center) value='date') as |calendar|}}
  {{calendar.nav}}

  {{#calendar.days as |day|}}
    {{day.number}}
  {{/calendar.days}}
{{/power-calendar}}
